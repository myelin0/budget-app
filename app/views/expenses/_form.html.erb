<div class="container">
<%= form_with model: @expense, url: category_expenses_path, method: :post do |form| %>
  <% if expense.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
        <% expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class:"form-control mb-2" %>
  </div>

  <div class="form-group">
    <%= form.label :amount, style: "display: block" %>
    <%= form.number_field :amount, class:"form-control mb-2" %>
  </div>

  <div>
    <%= form.hidden_field :category_id, {value: @category_id} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
</div>
